/* 📖 문법 안내서 — 다른 LLM에게 통째로 건네주는 설명서.
 *
 * 이 파일의 목적은 하나다.
 *   **클로드·챗GPT·제미나이에게 이 글을 주면 바로 쓸 수 있는 대본을 써 오게 하는 것.**
 *
 * 그래서 세 가지를 지킨다.
 *   ① 손으로 적지 않는다 — 낱말은 전부 실제 코드에서 뽑아낸다. 낡을 수가 없다.
 *      쓸 수 있는 낱말은 검사기(lint.js)의 `낱말표` 를 그대로 읽는다.
 *      **설명서에 적힌 것 = 검사기가 통과시키는 것** 이 언제나 같아야 하기 때문이다.
 *   ② '이 컴퓨터에서만 되는 것'을 분명히 적는다 — 바깥 LLM 은 우리 자산 목록도,
 *      효과음 이름도, 그림이 없으면 코드로 그려진다는 사실도 모른다.
 *   ③ **틀리는 방식**을 적는다. 실제로 밖의 LLM 이 써 온 대본에서 걸린 것들이다.
 *      마크다운 구분선, 없는 낱말, 자막 꾸미기를 첫머리에만 적는 버릇 —
 *      전부 "영상은 나오는데 뜻대로가 아닌" 실패라 알려 주지 않으면 계속 틀린다.
 */
import { EFFECTS } from "./fx.js";
import { SPOTS } from "./script.js";
import { sceneryWords, colorWords } from "./scenery.js";
import { PROPS } from "./props.js";
import { STATES } from "../char/moods.js";
import { CHARACTER_WORDS } from "../char/autochar.js";
import { BODY_MOVES, EXPRESSIONS, TEARS } from "../char/face.js";
import { library } from "./assets.js";
import { PRESETS, BACKINGS, FONT_NAMES, 기본모양 } from "./subtitle.js";
import { 낱말표 } from "./lint.js";

/** 문법이 바뀔 때마다 올린다 (LLM 이 어떤 판을 보고 썼는지 알 수 있게) */
export const GUIDE_VERSION = "2.1";   // 2.0 프로젝트/이야기 문법 분리·자막 장 / 2.1 빠진 문법 채움

const 줄 = (...a) => a.join("\n");
const 표 = arr => (arr || []).map(x => `\`${x}\``).join(" · ");
/** 검사기가 통과시키는 낱말을 그대로 — 설명서와 검사기가 어긋날 수 없게 */
const 목록 = 갈래 => 표(낱말표[갈래]?.값 || []);

/**
 * 전체 문법을 마크다운으로. 내 자산 목록까지 넣어 준다.
 * @param opt {소리:[이름], group:시리즈아이디}
 */
export async function buildGuide(opt = {}) {
  // 이 시리즈에서 쓸 수 있는 자산만 싣는다 — 없는 것을 부르는 대본이 오면 안 된다
  const 그룹 = opt.group ?? (localStorage.getItem("ws그룹") || "");
  let 시리즈이름 = "";
  if (그룹 && 그룹 !== "__none") {
    try {
      const g = (await (await fetch("/api/group/list")).json()).items || [];
      시리즈이름 = (g.find(x => x.id === 그룹) || {}).name || "";
    } catch {}
  }
  let 소리 = opt.소리;
  if (!소리) {
    try { 소리 = (await (await fetch("/api/sfx/list")).json()).names || []; } catch { 소리 = []; }
  }
  /* ── 쓸 수 있는 자산을 **자세히** 모은다 ──
   *
   * 이름만 적어 주면 밖의 LLM 이 대본을 못 쓴다.
   * 캐릭터에 어떤 포즈가 있는지 모르면 `<비몽> 포즈 sleep` 을 쓸 수가 없다.
   * 그래서 포즈·태그·길이까지 실어 보낸다.
   */
  let 캐릭터 = [], 배경 = [], 음악 = [], 조각 = [], 공용수 = 0;
  try {
    const q = 그룹 && 그룹 !== "__none" ? `?group=${encodeURIComponent(그룹)}` : "";
    const [g, ch, bg, au, pf] = await Promise.all([
      fetch("/api/group/assets" + q).then(r => r.json()),
      fetch("/api/char/list").then(r => r.json()).catch(() => ({})),
      fetch("/api/bg/list").then(r => r.json()).catch(() => ({})),
      fetch("/api/audio/list").then(r => r.json()).catch(() => ({})),
      fetch("/api/prefab/list").then(r => r.json()).catch(() => ({})),
    ]);
    const 것들 = g.items || [];
    공용수 = 것들.filter(x => x.공용).length;
    /* 종류별 원본에서 '더 아는 것'(포즈·길이)을 끌어와 붙인다.
       group/assets 는 이름·태그만 주기 때문이다. */
    const 짝 = (종류, 원본, 더) => 것들.filter(x => x.종류 === 종류).map(x => {
      const o = (원본 || []).find(y => String(y.id) === String(x.id)) || {};
      return { 이름: x.name, 공용: x.공용, tags: x.tags || [], ...더(o) };
    });
    캐릭터 = 짝("캐릭터", ch.items, o => ({ 포즈: Object.keys(o.poses || {}) }));
    배경 = 짝("배경", bg.items, o => ({ 출처: o.출처 || "" }));
    음악 = 짝("음악", au.items, o => ({ 초: o.초 || 0 }));
    조각 = 짝("조각", pf.items, o => ({ 줄수: String(o.text || "").split("\n").filter(s => s.trim()).length }));
  } catch {
    try {
      await library.refresh();
      캐릭터 = library.characters.map(c => ({ 이름: c.name, 포즈: Object.keys(c.poses || {}) }));
      배경 = library.backgrounds.map(b => ({ 이름: b.name }));
    } catch {}
  }
  /* 목록 한 줄 만들기 — 태그가 있으면 뒤에 붙여 준다 (밖의 LLM 이 골라 쓰기 쉽게) */
  const 줄로 = (x, 덧 = "") =>
    `- \`${x.이름}\`${x.공용 ? " *(공용)*" : ""}${덧}` +
    ((x.tags || []).length ? `  — ${x.tags.join(", ")}` : "");

  const 이제 = new Date();
  const 날짜 = `${이제.getFullYear()}-${String(이제.getMonth() + 1).padStart(2, "0")}-` +
               `${String(이제.getDate()).padStart(2, "0")}`;

  /* 예시에 쓸 실제 이름 — 없는 이름으로 예시를 들면 그대로 베껴 쓴다 */
  const 예캐릭 = 캐릭터[0]?.이름 || "누렁이";
  const 예포즈캐릭 = 캐릭터.find(c => c.포즈?.length > 1);
  const 예배경 = 배경[0]?.이름 || "공원 잔디밭";
  const 예음악 = 음악[0]?.이름 || "산책배경음";
  const 기본 = 기본모양();

  return 줄(
`# 심심 공작소 대본 문법 (v${GUIDE_VERSION} · ${날짜} 기준)${
  시리즈이름 ? `

> **시리즈: ${시리즈이름}**  이 글에 적힌 자산은 이 시리즈에서 쓸 수 있는 것뿐입니다.` : ""}

이 글은 **영상 대본을 쓰는 규칙**입니다. 이 규칙대로 쓴 글을 프로그램에 붙여 넣으면
사람 손을 거치지 않고 그대로 영상이 됩니다.
아래 낱말은 전부 이 컴퓨터에 **실제로 들어 있는 것**에서 뽑았습니다.

---

# 📌 0. 먼저 읽어 주세요 (대본을 써 주는 분께)

## 지켜야 할 것 — 이것만 지키면 반드시 영상이 나옵니다

1. **대본은 순수한 글줄입니다. 마크다운이 아닙니다.**
   각 줄은 명령 한 개입니다. 꾸밈을 넣지 마세요.
2. **설명·구간표시·제목을 줄로 넣지 마세요.** 넣고 싶으면 줄 앞에 \`#\` 를 붙이세요.
3. **개체(캐릭터·배경·소품)는 꺾쇠로 감쌉니다.** \`<${예캐릭}>\`, \`장면 <${예배경}>\`
4. **화면에 나올 글은 따옴표 안에.** \`자막 "안녕하세요"\`
5. **시간에는 반드시 \`초\` 를 붙입니다.** \`2초\` — 그냥 \`2\` 는 각도·배율로 읽습니다.
6. **낱말은 이 글에 적힌 목록에서만 고릅니다.**
   목록에 없는 낱말은 **오류가 나지 않습니다.** 그냥 아무 일도 안 일어납니다.
   \`동작 sway2\` 라고 쓰면 캐릭터는 가만히 있고, 그걸 알아챌 방법이 없습니다.
7. **자산 이름은 이 글 아래 〈쓸 수 있는 자산〉에서 그대로 베껴 씁니다.**
   한 글자만 달라도 다른 것이 됩니다.

## ⛔ 이렇게 쓰면 안 됩니다 (실제로 자주 나온 실수)

| 이렇게 쓰면 | 무슨 일이 나나 | 이렇게 쓰세요 |
|---|---|---|
| \`───────── 0~7초 · 도입부\` | 명령으로 읽혀 **오류** | \`# 0~7초 도입부\` |
| \`## 장면 1\` \`**장면 <숲>**\` | 꾸밈이 섞여 못 알아봄 | \`장면 <숲>\` |
| \`- 자막 "안녕"\` (글머리표) | 목록으로 오해 | \`  자막 "안녕"\` |
| \`대기 3\` (초 빠짐) | 3이 무시되고 1초가 됨 | \`대기 3초\` |
| \`효과 반짝반짝\` | 없는 이름 → **아무 효과 없음** | \`효과 반짝임\` |
| \`<A> 등장 한가운데\` | 없는 자리 → 기본 자리로 | \`<A> 등장 가운데\` |
| \`자막 "긴 글" 크기:0.14\` | 화면 밖으로 나감 | 글을 짧게 나누기 |
| 자막이 하나도 없음 | **0초 영상** — 굽기가 막힙니다 | 자막을 넣으세요 |

> 프로그램이 웬만한 꾸밈은 알아서 걷어 내지만(구분선·글머리표·\`**굵게**\`),
> **기대지 마세요.** 처음부터 깨끗하게 쓰는 것이 확실합니다.

## ✅ 대본을 넘기기 전 스스로 확인할 것

- [ ] 명령이 아닌 줄이 하나도 없는가 (설명은 전부 \`#\` 뒤로)
- [ ] 모든 \`<개체>\` 이름이 〈쓸 수 있는 자산〉 목록에 그대로 있는가
- [ ] 모든 효과·상태·동작·전환·분위기·자리 낱말이 목록에 있는가
- [ ] 모든 시간에 \`초\` 가 붙어 있는가
- [ ] 따옴표와 꺾쇠의 짝이 다 맞는가
- [ ] 자막이 한 줄이라도 있는가 (없으면 0초짜리가 됩니다)
- [ ] 목표 길이에 맞는 자막 줄 수인가 (아래 1장 계산법)

---

# 🎬 1. 이 프로그램이 영상을 만드는 방식

## 작은 것을 이어 붙여 큰 것을 만듭니다

이 프로그램에는 '타임라인' 이 없습니다. **글을 위에서 아래로 읽으며 시간을 밀 뿐**입니다.
그래서 층이 네 겹입니다.

\`\`\`
컷  ── 자막 한 줄 = 컷 하나 (1.4~6초)
 └ 장면 ── 배경 하나 = 장면 하나 (컷 2~5개)
    └ 편(이야기) ── 영상 파일 하나 (장면 4~25개)
       └ 이어붙이기 ── 편들을 이어 붙여 긴 영상 하나
\`\`\`

## 시간을 미는 것은 네 가지뿐입니다

\`\`\`
자막 · 대사 · 대기 · 멈칫
\`\`\`

나머지(\`등장\`·\`이동\`·\`표정\`·\`효과\`·\`카메라\`·\`화면\`)는 **지금 시각에 시작해
제 시간만큼 흐를 뿐, 다음 줄을 기다리게 하지 않습니다.**

\`\`\`
자막 "비가 온다"          # 여기서 2.4초가 흐른다
<${예캐릭}> 이동 왼쪽 3초    # 이 줄은 시간을 안 민다 — 아래 자막과 겹쳐 흐른다
자막 "우산을 폈다"         # 이동이 끝나기 전에 이 자막이 시작된다
\`\`\`

이것이 이 문법의 **가장 중요한 성질**입니다.
움직임을 자막과 겹쳐 흐르게 해서, 짧은 글로도 화면이 계속 살아 있게 만듭니다.

## 자막 길이는 저절로 정해집니다

글자 수로 계산합니다 — \`1.0초 + 글자수 × 0.13초\` (최소 1.4초, 최대 6초).
10글자면 2.3초, 20글자면 3.6초입니다.
바꾸고 싶으면 뒤에 적으세요 — \`자막 "짧게 스쳐" 1초\`

## 📐 길이 계산법 — 몇 줄을 써야 하나

| 만들 것 | 목표 길이 | 자막 줄 수 | 장면 수 | 편 수 |
|---|---|---|---|---|
| 숏츠 (유튜브·릴스) | 30초 | **10 ~ 14줄** | 4 ~ 6 | 1편 |
| 짧은 이야기 | 1분 | 20 ~ 28줄 | 6 ~ 10 | 1편 |
| 이야기 한 편 | 3분 | 60 ~ 80줄 | 15 ~ 25 | 1편 |
| 긴 영상 (이어붙이기) | 20 ~ 30분 | — | — | **10 ~ 20편** |

> 어림셈: **자막 한 줄 ≈ 2.4초**. 30초를 채우려면 12줄쯤입니다.
> 굽는 데 GPU 를 쓰지 않아 30초가 5~10초면 구워집니다. **길이를 아끼지 마세요.**

## ✍ 좋은 대본으로 만드는 법

**장면 나누기**
- **배경이 바뀔 때만 장면을 나눕니다.** 같은 배경인데 장면을 나누면 화면이 덜컹거립니다.
- 한 장면에 자막 2~5줄이 알맞습니다. 한 줄짜리 장면만 이어지면 정신없습니다.
- 장면마다 \`분위기:\` 를 정해 주세요. 색보정·비네트·블룸이 한꺼번에 걸려 화질이 확 달라집니다.

**컷 리듬**
- 긴 자막과 짧은 자막을 섞으세요. 다 비슷하면 지루합니다.
- 놀람·전환점 앞에 \`멈칫 0.2초\` 를 넣으면 타격감이 생깁니다.
- 3~4컷마다 카메라를 한 번 움직이세요 (\`카메라 줌인 2초\`). 정지 그림이 살아납니다.

**숏츠(30초)라면**
- **첫 3초 안에 무슨 이야기인지 보여 주세요.** 첫 자막이 곧 후킹입니다.
- 마지막 컷은 다음 편이 궁금해지게 끝내세요.
- 처음과 끝을 같은 배경으로 맞추면 반복 재생이 매끄럽습니다.

**여러 편을 이어 붙일 거라면**
- 앞 편의 마지막 배경과 다음 편의 첫 배경을 같게 하면 자연스럽게 이어집니다.
- 편마다 \`제목:\` 을 꼭 적으세요 — 굽기·이어붙이기에서 그 이름으로 고릅니다.
- 편 사이 간격은 프로젝트에서 \`이어붙이기: 사이 0.4초\` 로 정합니다.

---

# 📁 2. 【프로젝트 문법】 — 한 번만 적는 것

프로젝트는 **여러 편을 담는 그릇**입니다. 여기 적은 것은 모든 편에 걸립니다.
편은 \`== 이야기 이름\` 으로 나눕니다.

\`\`\`
프로젝트: ${시리즈이름 || "댕댕이 시리즈"}
비율: 9:16
해상도: 1280
초당장수: 30
화질: 보통
배경색: #101018
음악: ${예음악}   소리크기:0.7  여닫이:1.5초
음악프롬프트: warm acoustic ukulele, cozy, gentle 90bpm, no vocals
이어붙이기: 사이 0.4초
굽기: 둘다

== 이야기 1화 첫 만남
… (아래 3장 이야기 문법)

== 이야기 2화 밤바다 산책
…
\`\`\`

| 적는 말 | 뜻 | 고를 수 있는 것 |
|---|---|---|
| \`프로젝트:\` (= \`제목:\`) | 프로젝트 이름 | 아무 글 |
| \`비율:\` | 화면 비율 | ${목록("비율")} |
| \`해상도:\` (= \`긴변:\`) | 긴 변 크기 | \`720\` · \`1080\` · \`1280\` · \`1920\` |
| \`초당장수:\` (= \`fps:\`) | 초당 프레임 | \`24\` · \`30\` · \`60\` |
| \`화질:\` | 굽기 품질 | ${목록("화질")} |
| \`배경색:\` | 모든 장면의 바탕색 | 색 코드 |
| \`음악:\` | 배경 음악 (자산 이름) | 아래 목록에서 |
| \`음악프롬프트:\` | Suno 에 넣을 **영어** 설명 | 아무 글 |
| \`음악분위기:\` | Suno 프롬프트에 \`mood:\` 로 붙습니다 | 아무 글 |
| \`이어붙이기:\` | 편 사이 간격 | \`사이 0.4초\` · \`안함\` |
| \`굽기:\` | 무엇을 구울지 | 아래 참고 |
| \`메모:\` | 사람이 볼 메모 (영상에 안 나옴) | 아무 글 |
| \`자막차림표:\` \`자막글꼴:\` … | **모든 편에 저절로 내려갑니다** | 아래 4장 |

편을 나누는 말은 \`== 이야기 이름\` 이 기본이고, \`== 스토리 …\` · \`== 편 …\` ·
그냥 \`== 이름\` 도 같습니다.

## \`굽기:\` — 무엇을 구울지 고르기

\`\`\`
굽기: 각각        편마다 한 개씩 (숏츠·릴스)
굽기: 이어서      전부 이어 붙여 한 개 (20~30분 긴 영상)
굽기: 둘다
굽기: 안함        구조만 만들고 굽지 않음
굽기: 1화         그 편만 (이름 일부만 적어도 찾습니다)
굽기: 1화, 3화    고른 편만
\`\`\`

한 편만 고쳤을 때 전부 다시 구울 까닭이 없습니다.
🔥 굽기 화면에서도 편마다 [🔥 굽기] · [▶ 보기] · [🎞 컷 보기] · [⬇ 내려받기] 를 누를 수 있습니다.

---

# 📜 3. 【이야기 문법】 — 편마다 적는 것

한 편이 곧 영상 파일 하나입니다. 편 하나만 따로 쓸 때도 이 문법 그대로입니다.

\`\`\`
제목: 1화 첫 만남
자막차림표: 유튜브        # 이 편의 자막 기본 모양
분위기: 아침              # 이 편의 기본 분위기
여닫이: 어둡게            # 영상 앞뒤를 어둠에서 열고 닫기

장면 <${예배경}>   전환:페이드   분위기:아침
  <${예캐릭}> 등장 가운데 크기:0.7 동작:breathe
  자막 "안녕! 나는 ${예캐릭}야"
  <${예캐릭}> 상태 기쁨
  자막 "오늘은 뭘 해 볼까?"
\`\`\`

| 적는 말 | 뜻 | 고를 수 있는 것 |
|---|---|---|
| \`제목:\` | 이 편의 이름 | 아무 글 |
| \`분위기:\` | 이 편의 기본 분위기 | ${목록("분위기")} |
| \`여닫이:\` | 앞뒤를 어둠·빛에서 열고 닫기 | ${목록("여닫이")} |
| \`자막차림표:\` \`자막글꼴:\` … | 이 편의 자막 기본 모양 | **아래 4장에 전부** |
| \`음악:\` | 이 편만 다른 음악 | 아래 목록에서 |
| \`비율:\` | 이 편만 다른 비율 | ${목록("비율")} |
| \`배경색:\` | 모든 장면의 바탕색 | 색 코드 |

> ⚠ **편 첫머리에서 안 먹는 것** — \`해상도:\` \`초당장수:\` \`화질:\` 은 프로젝트 것만 쓰입니다.
> 편에 적어도 조용히 무시되니 프로젝트 첫머리에 적으세요.

> ⚠ \`여닫이\` 는 두 군데에 쓰이고 뜻이 다릅니다.
> 첫머리의 \`여닫이: 어둡게\` 는 **화면**을 여닫는 것이고,
> \`음악: … 여닫이:1.5초\` 는 **소리**를 서서히 키우고 줄이는 **초**입니다.

## 장면

\`\`\`
장면 <배경이름>   전환:페이드   분위기:밤   전환길이:0.8초
\`\`\`

| 꼬리표 | 뜻 |
|---|---|
| \`전환:\` | ${목록("전환")} |
| \`전환길이:\` | 넘어가는 데 걸리는 초 (기본 0.6초) |
| \`분위기:\` | ${목록("분위기")} |
| \`길이:3\` | 장면 길이를 초로 못 박기 (안 적으면 자막 길이의 합) |
| \`배경색:\` | 이 장면만 바탕색 (\`바탕:\` 도 같은 말) |
| \`뒤:\` \`앞:\` | 겹배경 (\`뒷배경:\` \`앞배경:\` 도 같은 말) |
| \`칸:테두리\` \`칸:굵게\` | 만화 칸 테두리 |
| \`인서트:0.34\` | 구석에 확대컷 · \`인서트자리:왼쪽\` \`인서트배율:2\` 로 조절 |

> **첫 장면에는 전환이 안 걸립니다.** 안 적으면 두 번째 장면부터 페이드로 넘어갑니다.
> 첫 장면부터 어둠에서 열고 싶으면 편 첫머리에 \`여닫이: 어둡게\` 를 쓰세요.

## 배우

\`\`\`
<이름> 등장 가운데 크기:0.7 표정:happy 동작:breathe
<이름> 이동 왼쪽 2초
<이름> 이동 왼쪽,가운데,오른쪽 3초      # 여러 곳을 거쳐
<이름> 점프 가운데 0.8초 높이:0.2
<이름> 떨기 0.6초 세기:1.5
<이름> 기울이기 15 0.5초                # 15는 각도
<이름> 방향 왼쪽
<이름> 크기 1.2 0.5초
<이름> 눌리기 0.3초 세기:1.2            # 납작 (부피를 지킵니다)
<이름> 늘어나기 0.25초                  # 홀쭉
<이름> 대사 "안녕!"   소리:딩            # 대사는 시간을 밉니다
<이름> 퇴장
\`\`\`

\`이동\` 에는 두 가지를 더 붙일 수 있습니다.
- \`부드럽게\` 또는 \`방식:튐\` — 어떻게 움직이나 (\`linear\` · \`ease\` · \`in\` · \`out\` · \`튐\`)
- \`준비:없음\` — 떠나기 전 살짝 뒤로 빼는 예비동작을 끕니다

### 자리
${목록("자리")}
숫자로도 씁니다: \`0.5,0.7\` (왼쪽 0 ~ 오른쪽 1, 위 0 ~ 아래 1)
\`점프\` 에만 \`제자리\` 를 쓸 수 있습니다 — \`<이름> 점프 제자리 0.6초\`

### 상태 — 한 낱말로 한꺼번에 (가장 권합니다)

\`<이름> 상태 놀람\` 한 줄이 표정·동작·눈물·효과·눌림·떨기·카메라·소리를 함께 겁니다.
표정과 동작을 따로 적는 것보다 이 쪽이 훨씬 잘 나옵니다.

${Object.entries(STATES).map(([k, v]) => `- \`${k}\` — ${v.ko}`).join("\n")}

### 포즈 — 올려 둔 그림 갈아 끼우기

한 캐릭터에 그림이 여러 장 있으면 그중 하나로 바꿉니다.

\`\`\`
${예포즈캐릭 ? `<${예포즈캐릭.이름}> 포즈 ${예포즈캐릭.포즈[0]}` : "<비몽> 포즈 sleep"}
${예포즈캐릭 ? `<${예포즈캐릭.이름}> 모습 ${예포즈캐릭.포즈[1] || 예포즈캐릭.포즈[0]}` : "<비몽> 모습 angry"}        # '모습' 도 같은 말입니다
\`\`\`

- 그 줄부터 **끝까지** 그 그림으로 있습니다 (다시 \`포즈\` 를 써야 바뀝니다).
- 어떤 포즈가 있는지는 아래 〈쓸 수 있는 자산〉에 캐릭터마다 적혀 있습니다.
- **없는 포즈 이름을 쓰면 기본 그림 그대로입니다** — 아무 일도 안 일어납니다.
- 그림이 한 장뿐인 캐릭터에는 아무 일도 안 일어납니다.

**\`상태\` 와 무엇이 다른가** — \`상태\` 는 코드로 표정·움직임을 입히는 것이고,
\`포즈\` 는 **올려 둔 다른 그림으로 갈아 끼우는 것**입니다.
표정이 이미 그려진 그림 묶음이 있다면 \`포즈\` 가 훨씬 잘 나옵니다.

### 표정 · 몸 움직임 · 눈물 (따로 쓸 때)

\`\`\`
<이름> 표정 happy 팝
<이름> 표정 sad 서서히 0.6초
<이름> 표정 angry 즉시
<이름> 표정반복 happy,surprised 0.4초
<이름> 동작 bounce
<이름> 눈물 roll
\`\`\`

- 표정 ${목록("표정")}
- 표정 바꾸는 방식 \`팝\` · \`서서히\` · \`즉시\` (안 적으면 즉시)
- 동작 ${목록("동작")}
- 눈물 ${목록("눈물")}

## 소품 (물건 개체)

\`\`\`
소품 <공>                       # 선언하면 배우와 똑같은 지시를 다 씁니다
소품 <내 보물> 모양:별           # 이름과 모양이 다를 때
<공> 등장 오른쪽 크기:0.2
<공> 점프 왼쪽 0.8초             # 던지기
<공> 돌리기 2 1초                # 빙글빙글
<공> 붙이기 <${예캐릭}> 손         # 손·머리·등·입·발·가운데
<공> 떼기
\`\`\`

그릴 수 있는 모양: ${목록("소품")}
소품은 **그림이 없는 것이 정상**입니다 — 코드가 그립니다.
크기는 \`소품\` 줄이 아니라 **\`등장\` 줄의 \`크기:\`** 로 정합니다.
붙일 수 있는 자리: \`손\` · \`머리\` · \`등\` · \`입\` · \`발\` · \`가운데\`
(\`붙이기\`·\`떼기\` 는 \`들기\`·\`놓기\` 라고 써도 같습니다.)

## 카메라

\`\`\`
카메라 줌인 2초 세기:1.25
카메라 줌아웃 1.5초
카메라 이동 오른쪽 2초
카메라 흔들기 0.5초 세기:1.4      # 세기의 제곱으로 흔들려 더 세게 느껴집니다
카메라 기울이기 8 0.5초           # 불안·긴장 (더치 앵글)
카메라 펀치 0.3초                 # 확 당겼다 돌아오기 (놀람)
카메라 레터박스 0.5초             # 위아래 검은 띠 (회상)
카메라 레터박스 끄기              # 띠 없애기
카메라 따라가기 ${예캐릭} 무시:0.1 느슨:0.9   # 배우를 부드럽게 쫓아감
카메라 되돌리기 1초
\`\`\`

- 쓸 수 있는 움직임: ${목록("카메라")}
- \`카메라 이동\` 은 **가로만** 움직입니다 (위아래는 그대로).
- \`따라가기\` 의 \`무시:\` 는 이만큼 움직여야 쫓기 시작한다는 뜻,
  \`느슨:\` 은 얼마나 늦게 따라붙나입니다 (1에 가까울수록 느긋).
- 움직이고 나면 그대로 있습니다 — 원래대로 돌리려면 \`카메라 되돌리기 1초\`.

## 화면(후처리) · 시간 · 효과 · 소리

\`\`\`
화면 분위기 무서움
화면 비네트 0.5 1초      # 가장자리 어둡게
화면 블룸 0.6 1.5초      # 밝은 곳 번짐
화면 결 0.2              # 필름 알갱이
화면 번쩍 0.2초          # 한순간 하얗게

느리게 0.4 1초           # 슬로모션
빠르게 2 0.6초
보통속도 0.3초
멈칫 0.2초               # 아예 얼리기 (타격감) — 시간을 밉니다
대기 1.5초               # 시간을 밉니다

효과 반짝임 위치:0.5,0.4 크기:1.2
효과 눈 길이:3초 색:#cfe6ff 세기:1.4 방향:0.3
소리 딩
\`\`\`

- 화면 ${목록("화면")} — \`화면 밤\` 처럼 분위기 이름을 바로 써도 됩니다
- 효과 ${목록("효과")}
- 효과 꼬리표 \`위치:\` \`크기:\` \`색:\` \`세기:\` \`방향:\` \`길이:\`

> ⚠ **효과에 \`길이:\` 를 안 적으면 장면이 끝날 때까지 계속됩니다.**
> 한순간만 반짝이게 하려면 \`효과 반짝임 길이:0.6초\` 처럼 꼭 적으세요.
- 효과음 (이 컴퓨터에 있는 것, ${소리.length}개)
  ${소리.length ? 표(소리) : "_(목록을 못 읽었습니다)_"}

## 같은 뜻인 다른 이름들

둘 중 아무거나 써도 됩니다. **이 표에 없는 비슷한 말은 안 통합니다.**

| 쓰는 말 | 같은 말 |
|---|---|
| \`등장\` | \`나옴\` |
| \`이동\` | \`걸어감\` |
| \`대사\` | \`말\` |
| \`점프\` | \`폴짝\` |
| \`떨기\` | \`부들부들\` |
| \`기울이기\` | \`갸웃\` |
| \`방향\` | \`돌아보기\` |
| \`퇴장\` | \`사라짐\` |
| \`포즈\` | \`모습\` |
| \`붙이기\` / \`떼기\` | \`들기\` / \`놓기\` |
| \`표정반복\` | \`표정번갈아\` |
| \`대기\` | \`쉼\` |
| \`소품\` | \`물건\` |
| \`화면\` | \`화면효과\` |
| \`차림표:\` | \`모양:\` |

꺾쇠는 안 감싸도 됩니다 (\`장면 밤바다\`, \`소품 공\`). 다만 **감싸는 쪽이 확실합니다** —
띄어쓴 이름이나 동사와 같은 이름일 때 어디서 끊길지 프로그램이 헷갈립니다.

## 🧩 조각 — 대본 토막 다시 쓰기

자주 쓰는 몇 줄을 이름 붙여 두고 어디서든 불러 넣습니다. 시리즈물에서 특히 요긴합니다.

\`\`\`
조각 <아침 인사>
\`\`\`

그 자리에 조각에 적힌 줄들이 통째로 들어갑니다 (조각 안에서 다른 조각도 부를 수 있습니다).

---

# 💬 4. 자막 — 이 프로그램에서 가장 중요한 것

**목소리(TTS)가 없습니다.** 말은 전부 자막과 말풍선으로 합니다.
그래서 **자막이 읽히는가가 곧 영상 품질**입니다. 대본에 적은 대로 정확히 그려집니다.

## 어디에 적나 — 세 곳

\`\`\`
① 프로젝트 첫머리   자막차림표: 유튜브        ← 모든 편에 저절로 내려갑니다
② 편 첫머리         자막차림표: 속삭임        ← 이 편 전체
③ 한 줄 뒤          자막 "이 줄만" 크기:0.1   ← 그 줄만
\`\`\`

**앞에 \`자막\` 을 붙이면 첫머리용, 안 붙이면 그 줄용**입니다.
\`자막글꼴: 또박\` (첫머리) ↔ \`자막 "…" 글꼴:또박\` (한 줄).

### ⚠ 한 줄에 \`차림표:\` 를 쓰면 앞의 설정이 **전부** 지워집니다

낱낱이 적은 것끼리는 뒤엣것이 이기지만, **\`차림표:\` 만은 한 벌을 통째로 덮어씁니다.**

\`\`\`
자막글꼴: 또박            # 편 전체
자막색: #00ff00

  자막 "여기는 또박·초록"                    → 또박 · 초록  ✅
  자막 "여기는?"  차림표:예능                 → 굵게 · 노랑  (또박·초록이 **사라짐**)
  자막 "이렇게"   차림표:예능 글꼴:또박        → 굵게가 아니라 또박 · 노랑 (되살리려면 다시 적기)
\`\`\`

한 줄만 살짝 다르게 하려면 \`차림표:\` 말고 **바꿀 항목만** 적으세요 — \`크기:0.1 색:#ffe14d\`.

## 차림표를 먼저 고르세요 — 한 벌이 통째로 정해집니다

| 차림표 | 어떤 모양인가 | 어울리는 곳 |
|---|---|---|
${Object.entries(PRESETS).map(([k, p]) =>
  `| \`${k}\` | ${p.ko} — ${p.값.글꼴} 글꼴, 크기 ${p.값.크기}, 바탕 ${p.값.바탕}${
    p.값.테두리 ? `, 테두리 ${p.값.테두리}` : ""} | ${
    { "종이": "동화·잔잔한 이야기", "유튜브": "정보·설명",
      "영화": "서사·감성", "예능": "웃음·강조",
      "만화": "말풍선 느낌", "속삭임": "밤·꿈·독백" }[k] || "" } |`).join("\n")}

\`\`\`
자막차림표: 유튜브                    # 이 편 전체
자막 "이 줄만 크게!"  차림표:예능      # 이 줄만
\`\`\`

## 낱낱이 고치기 — 모든 항목

| 한 줄에 적을 때 | 첫머리에 적을 때 | 고를 수 있는 것 | 기본값 |
|---|---|---|---|
| \`글꼴:\` | \`자막글꼴:\` | ${FONT_NAMES.join(" · ")} | ${기본.글꼴} |
| \`크기:\` | \`자막크기:\` | 0.03 ~ 0.14 (화면 짧은 변 대비) | ${기본.크기} |
| \`색:\` | \`자막색:\` | \`#ffffff\` 같은 색 코드 | \`${기본.색}\` |
| \`가로:\` | \`자막가로:\` | ${목록("자막가로")} | ${기본.가로} |
| \`세로:\` | \`자막세로:\` | ${목록("자막세로")} | ${기본.세로} |
| \`높이:\` | \`자막높이:\` | 0(맨 위) ~ 1(맨 아래) — 적으면 \`세로:\` 를 이깁니다 | 없음 |
| \`바탕:\` | \`자막바탕:\` | ${Object.entries(BACKINGS).map(([k, b]) => `\`${k}\`(${b.설명})`).join(" · ")} | ${기본.바탕} |
| \`바탕색:\` | \`자막바탕색:\` | 색 코드 | \`${기본.바탕색}\` |
| \`투명도:\` | \`자막투명도:\` | 0(다 비침) ~ 1(꽉 참) | ${기본.투명도} |
| \`테두리:\` | \`자막테두리:\` | 0 ~ 0.3 — 글자 둘레를 파 줍니다 | ${기본.테두리} |
| \`테두리색:\` | \`자막테두리색:\` | 색 코드 | \`${기본.테두리색}\` |
| \`등장:\` | \`자막등장:\` | ${목록("자막등장")} | ${기본.등장} |
| \`타자속도:\` | \`자막타자속도:\` | 초당 글자 수 (\`등장:타이핑\` 일 때만) | ${기본.타자속도} |
| \`차림표:\` | \`자막차림표:\` | ${목록("차림표")} | 없음 |
| 그 줄에만 | — | \`길이:2초\` · \`소리:톡\` · \`효과:반짝임\` | 자동 |

## 등장 방식

| 낱말 | 어떻게 나오나 |
|---|---|
| \`팝\` | 살짝 커지며 톡 (기본) |
| \`올라옴\` | 아래에서 밀려 올라옴 — 차분한 이야기 |
| \`튀어나옴\` | 튕기며 크게 — 예능·강조 |
| \`타이핑\` (= \`한글자씩\`) | 한 글자씩 찍힘 — 편지·독백 |

## 읽히게 만드는 법 (실전)

- **테두리가 가장 확실합니다.** \`테두리:0.12\` 를 주면 어떤 배경 위에서도 읽힙니다.
  바탕을 까는 것보다 그림을 덜 가립니다.
- **세로 영상은 \`높이:0.80\` 안팎**이 좋습니다. 더 내리면 유튜브 UI에 가립니다.
- **1초에 11자를 넘으면 못 읽습니다.** 길면 두 줄로 나누세요.
- 화면 위쪽에 중요한 것이 있으면 \`세로:위\` 또는 \`높이:0.16\`.
- 캐릭터가 하는 말이면 \`자막\` 대신 \`<이름> 대사 "…"\` 를 쓰세요 (말풍선이 붙습니다).

> ⚠ **말풍선(\`대사\`)에는 위 자막 꼬리표가 안 먹습니다.**
> \`차림표:\` \`테두리:\` \`높이:\` 같은 것을 붙여도 무시됩니다.
> 말풍선은 따로 \`글자크기:\` \`글꼴:\` \`색:\` \`바탕색:\` \`선색:\` 만 읽습니다.
> 자막처럼 꾸미고 싶으면 \`대사\` 말고 \`자막\` 을 쓰세요.

\`\`\`
자막차림표: 영화
자막테두리: 0.1
자막높이: 0.8

장면 <밤바다>   분위기:밤
  자막 "파도 소리가 좋아"
  자막 "여기 봐!"   가로:왼쪽 높이:0.2 색:#ffe14d 테두리:0.16 등장:튀어나옴
  자막 "…아무도 없네"   차림표:속삭임 등장:타이핑
\`\`\`

> 색 코드의 \`#\` 는 주석이 아닙니다. \`색:#ffffff\` 는 그대로 읽힙니다.
> 그 밖의 \`#\` 뒤는 설명으로 보고 버립니다.

**화면의 [💬 자막 꾸미기] 는 눈으로 확인하고 미세 조정하는 곳입니다.**
대본에 적은 것만으로 전부 정해지며, 거기서 고른 것은 대본 글로 다시 적혀 들어갑니다.

---

# 🖥 5. 이 컴퓨터에서만 되는 것

바깥에서 대본을 쓸 때는 모르는 사정입니다. **프로그램이 알아서 해 주는 부분**이라
대본에 억지로 채워 넣을 필요가 없습니다.

## 그림이 없어도 됩니다 (중요)

배경·캐릭터·소품은 **그림이 없으면 코드로 그려 줍니다.** 그래서 대본만 써도 영상이 나옵니다.
나중에 같은 이름으로 그림을 올리면 그 그림이 자동으로 대신 쓰입니다.

- **배경**: 이름의 낱말을 알아봅니다.
  시간대 ${표(sceneryWords().시간)}
  장소 ${표(sceneryWords().장소)}
  섞어 씁니다 — \`장면 <노을 지는 들판>\`, \`장면 <눈 오는 마을>\`
  단색도 됩니다 — \`장면 <흰색>\`, \`장면 <#204080>\`
  색 이름 ${표(colorWords())}
- **캐릭터**: 이름에서 동물을 알아봅니다 — ${표(CHARACTER_WORDS().map(w => w.split(" ")[0]))}
  못 알아보면 이름을 숫자로 바꿔 종류·색을 고릅니다 (같은 이름이면 언제나 같은 모습).
- **소품**: 위 모양 목록에서 고릅니다.

> 이 낱말들이 **실제로 어떻게 보이는지**는 프로그램의 **🎨 자산 → 🎬 연출** 에서
> 전부 움직이는 채로 볼 수 있습니다 (효과·소품·상태·표정·동작·눈물·분위기·전환·
> 자리·자막 모양·효과음). 칸을 누르면 대본에 적을 글이 복사됩니다.

## 굽기 전에 저절로 하는 점검

굽기를 누르면 먼저 살펴보고, **조용히 틀린 채 구워질 것**이 있으면 물어봅니다.

| 무엇을 보나 | 왜 |
|---|---|
| 시킨 음악이 정말 있나 | 이름이 한 글자만 틀려도 **소리 없이** 구워집니다 |
| 자막 차림표·글꼴이 있는 이름인가 | 없는 이름은 조용히 기본값이 됩니다 |
| 자막·대사가 하나라도 있나 | 없으면 읽을 것이 없는 영상이 됩니다 (굽기를 막습니다) |
| 자막이 화면 안에 들어오나 | 글이 길거나 크면 밖으로 나갑니다 |
| 읽을 시간이 있나 | 1초에 11자를 넘으면 못 읽습니다 |
| 없는 낱말을 썼나 | 그 지시는 **아무 일도 안 하고 사라집니다** |

## 음악 · 효과음 · 목소리

- 배경 음악은 Suno 같은 바깥 도구에서 뽑아 **🎨 자산 → 🎵 음악** 에 올려 둡니다.
  대본에서는 파일 이름이 아니라 **자산 이름**으로 부릅니다.
- 아직 없는 음악이면 \`음악프롬프트:\` 로 **어떤 음악이어야 하는지 영어로** 적어 주세요.
- **유튜브에 올릴 것이라면 라이선스가 없는 곡만** 씁니다.
- 효과음은 위 목록에 있는 것만 납니다 (전부 CC0 — 유튜브에 올려도 됩니다).
- **목소리(TTS)는 없습니다.** 말은 자막과 말풍선으로 합니다.

---

# 🗂 6. ${시리즈이름 ? `"${시리즈이름}" 시리즈에서 쓸 수 있는 자산` : "지금 올려져 있는 자산"}

여기 적힌 이름을 **그대로** 쓰면 올려 둔 그림·소리가 쓰입니다.
다른 이름을 써도 대본은 돌아가지만, 그때는 코드가 대신 그립니다.${시리즈이름 ? `
\`(공용)\` 은 모든 시리즈가 함께 쓰는 것입니다. 여기 **없는 이름은 이 시리즈에서 못 씁니다.**` : ""}

## 🐕 캐릭터 (${캐릭터.length}개)

${캐릭터.length ? 캐릭터.map(c => 줄로(c,
  c.포즈?.length > 1 ? `  · 포즈 ${c.포즈.length}개: ${c.포즈.map(p => `\`${p}\``).join(" ")}` : "")
).join("\n") : "_없음 — 이름만 적으면 코드가 그려 줍니다._"}
${예포즈캐릭 ? `
포즈가 여럿인 캐릭터는 \`포즈\` 로 골라 씁니다 — \`<${예포즈캐릭.이름}> 포즈 ${예포즈캐릭.포즈[0]}\`
(\`front\` 는 안 적어도 기본입니다. \`이동\` 하면 방향에 맞는 포즈를 알아서 씁니다.)` : ""}

## 🏞 배경 (${배경.length}개)

${배경.length ? 배경.map(b => 줄로(b)).join("\n") : "_없음 — 코드로 그린 배경 이름을 쓰세요._"}

## 🎵 음악 (${음악.length}개)

${음악.length ? 음악.map(m => 줄로(m, m.초 ? `  · ${Math.floor(m.초 / 60)}분 ${Math.round(m.초 % 60)}초` : "")).join("\n")
  : "_없음 — 소리 없이 나옵니다._"}

## 🧩 조각 (${조각.length}개)

${조각.length ? 조각.map(p => 줄로(p, p.줄수 ? `  · ${p.줄수}줄` : "")).join("\n") : "_없음_"}

---

# 🎯 7. 완성된 보기글 (30초 숏츠 · 그대로 써도 됩니다)

\`\`\`
제목: ${시리즈이름 || "누렁이"} 1화
비율: 9:16
음악프롬프트: warm sleepy lullaby, soft music box, gentle pad, no vocals
자막차림표: 속삭임
자막테두리: 0.12
자막높이: 0.8
여닫이: 어둡게

# ── 1장 · 열기 (0~8초)
장면 <${예배경}>   전환:페이드   분위기:아침
  <${예캐릭}> 등장 가운데 크기:0.72 동작:breathe
  자막 "아침 냄새가 났다"
  카메라 줌인 3초 세기:1.12
  자막 "오늘은 뭔가 다른 날 같아"
  <${예캐릭}> 상태 기쁨
  자막 "그런 예감이 들었다"

# ── 2장 · 사건 (8~20초)
장면 <노을 지는 들판>   전환:흐림   분위기:노을
  <${예캐릭}> 등장 왼쪽 크기:0.66
  <${예캐릭}> 이동 오른쪽 3초
  자막 "들판 끝까지 달렸다"
  효과 꽃잎 위치:0.5,0.1 크기:1.1
  자막 "바람이 등을 밀어 줬다"
  <${예캐릭}> 점프 가운데 0.7초 높이:0.22
  자막 "발이 땅에서 자꾸 떨어졌다"
  멈칫 0.2초
  <${예캐릭}> 상태 놀람
  카메라 펀치 0.3초
  자막 "…어라?"   차림표:예능 크기:0.1
  소리 뿅
  자막 "저 앞에 뭔가 있다"

# ── 3장 · 닫기 (20~30초)
장면 <밤하늘>   전환:어둡게   분위기:밤
  <${예캐릭}> 등장 0.5,0.66 크기:0.7
  화면 비네트 0.45 1.2초
  자막 "별이 다 켜져 있었다"
  효과 반짝임 위치:0.5,0.3 크기:1.0
  대기 0.6초
  자막 "하루가 조용히 접혔다"   등장:타이핑
  <${예캐릭}> 상태 졸림
  자막 "그래서 오늘은, 좋은 날"
  카메라 줌아웃 2초
\`\`\`

자막 12줄 · 장면 3개 ≈ 30초입니다.

---

# ✍ 마지막으로

1. 개체는 \`<꺾쇠>\` 로 감쌀 것
2. 시간에는 \`초\` 를 붙일 것
3. 효과·전환·상태·동작·자리·소품·소리는 **이 글의 목록에 있는 낱말만** 쓸 것
4. 자산 이름은 〈쓸 수 있는 자산〉에서 **그대로 베껴** 쓸 것
5. 자막 한 줄 = 컷 하나. 30초면 10~14줄
6. \`상태\` 를 적극 쓸 것 — 한 줄로 표정·동작·효과가 한꺼번에 걸립니다
7. 장면마다 \`분위기:\` 를 정해 줄 것 — 화질이 확 달라집니다
8. **명령이 아닌 줄은 쓰지 말 것.** 설명은 \`#\` 뒤에.
`);
}

/** 파일로 내려받기 — 시리즈를 주면 그 시리즈 자산만 담고 이름에도 넣는다 */
export async function downloadGuide(opt = {}) {
  const 글 = await buildGuide(opt);
  const 이제 = new Date();
  // 어느 시리즈 것인지 파일 이름만 봐도 알 수 있게 (여러 시리즈를 오갈 때 헷갈리지 않게)
  let 시리즈 = "";
  const gid = opt.group ?? (localStorage.getItem("ws그룹") || "");
  if (gid && gid !== "__none") {
    try {
      const g = (await (await fetch("/api/group/list")).json()).items || [];
      const n = (g.find(x => x.id === gid) || {}).name || "";
      if (n) 시리즈 = "_" + n.replace(/[\\/:*?"<>|\s]+/g, "");
    } catch {}
  }
  const 이름 = `심심공작소_대본문법${시리즈}_v${GUIDE_VERSION}_` +
    `${이제.getFullYear()}${String(이제.getMonth() + 1).padStart(2, "0")}` +
    `${String(이제.getDate()).padStart(2, "0")}.md`;
  const a = document.createElement("a");
  a.href = URL.createObjectURL(new Blob([글], { type: "text/markdown;charset=utf-8" }));
  a.download = 이름;
  a.click();
  setTimeout(() => URL.revokeObjectURL(a.href), 4000);
  return 이름;
}
